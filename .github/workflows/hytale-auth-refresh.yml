name: Hytale Auth Token Refresh

on:
  # Run every 30 minutes
  schedule:
    - cron: "*/30 * * * *"

  # Allow manual trigger with authentication inputs
  workflow_dispatch:
    inputs:
      hytale_access_token:
        description: "Hytale Access Token (for initial authentication)"
        required: false
        type: string
      hytale_refresh_token:
        description: "Hytale Refresh Token (optional)"
        required: false
        type: string
      force_rebuild:
        description: "Force rebuild hytale-auth container"
        required: false
        type: boolean
        default: false

jobs:
  refresh-auth:
    name: "Refresh Hytale Authentication"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Authentication
        id: setup_auth
        run: |
          # Use manual input if provided, otherwise use stored secret
          if [ -n "${{ inputs.hytale_access_token }}" ]; then
            echo "Using manually provided access token"
            ACCESS_TOKEN="${{ inputs.hytale_access_token }}"
            REFRESH_TOKEN="${{ inputs.hytale_refresh_token }}"
          else
            echo "Using stored access token from secrets"
            ACCESS_TOKEN="${{ secrets.HYTALE_ACCESS_TOKEN }}"
            REFRESH_TOKEN="${{ secrets.HYTALE_REFRESH_TOKEN }}"
          fi

          echo "access_token=$ACCESS_TOKEN" >> $GITHUB_OUTPUT
          echo "refresh_token=$REFRESH_TOKEN" >> $GITHUB_OUTPUT

      - name: Refresh OAuth2 Access Token
        id: refresh_token
        run: |
          echo "🔄 Refreshing OAuth2 access token using refresh_token grant..."

          # Use OAuth2 refresh token flow per Hytale documentation
          # https://support.hytale.com/hc/en-us/articles/45328341414043-Server-Provider-Authentication-Guide
          RESPONSE=$(curl -s -X POST "https://oauth.accounts.hytale.com/oauth2/token" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "client_id=hytale-server" \
            -d "grant_type=refresh_token" \
            -d "refresh_token=${{ steps.setup_auth.outputs.refresh_token }}")

          # Check if refresh was successful
          if echo "$RESPONSE" | jq -e '.error' >/dev/null 2>&1; then
            ERROR=$(echo "$RESPONSE" | jq -r '.error')
            ERROR_DESC=$(echo "$RESPONSE" | jq -r '.error_description // "No description provided"')
            echo "❌ Token refresh failed: $ERROR - $ERROR_DESC"
            exit 1
          fi

          # Extract new tokens
          NEW_ACCESS_TOKEN=$(echo "$RESPONSE" | jq -r '.access_token')
          NEW_REFRESH_TOKEN=$(echo "$RESPONSE" | jq -r '.refresh_token // ""')
          EXPIRES_IN=$(echo "$RESPONSE" | jq -r '.expires_in')

          # Use existing refresh token if new one wasn't provided
          if [ -z "$NEW_REFRESH_TOKEN" ] || [ "$NEW_REFRESH_TOKEN" == "null" ]; then
            NEW_REFRESH_TOKEN="${{ steps.setup_auth.outputs.refresh_token }}"
            echo "ℹ️  Keeping existing refresh token (new one not provided)"
          fi

          echo "new_access_token=$NEW_ACCESS_TOKEN" >> $GITHUB_OUTPUT
          echo "new_refresh_token=$NEW_REFRESH_TOKEN" >> $GITHUB_OUTPUT
          echo "✅ Access token refreshed successfully! (expires in ${EXPIRES_IN}s)"

      - name: Update GitHub Secrets
        if: steps.refresh_token.outputs.new_access_token != ''
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
          NEW_ACCESS_TOKEN: ${{ steps.refresh_token.outputs.new_access_token }}
          NEW_REFRESH_TOKEN: ${{ steps.refresh_token.outputs.new_refresh_token }}
        run: |
          # Update HYTALE_ACCESS_TOKEN secret using GitHub CLI
          echo "Updating HYTALE_ACCESS_TOKEN secret..."
          echo "$NEW_ACCESS_TOKEN" | gh secret set HYTALE_ACCESS_TOKEN --repo ${{ github.repository }}

          # Update HYTALE_REFRESH_TOKEN secret if it exists
          if [ -n "$NEW_REFRESH_TOKEN" ]; then
            echo "Updating HYTALE_REFRESH_TOKEN secret..."
            echo "$NEW_REFRESH_TOKEN" | gh secret set HYTALE_REFRESH_TOKEN --repo ${{ github.repository }}
          fi

          echo "✅ GitHub secrets updated successfully!"

      - name: Rebuild hytale-auth container
        if: inputs.force_rebuild || (github.event_name == 'workflow_dispatch' && inputs.hytale_access_token != '')
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: hytale-auth.yml
          token: ${{ secrets.PAT_TOKEN }}
          ref: main

      - name: Summary
        run: |
          echo "## 🎮 Hytale Authentication Refresh" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ OAuth2 tokens refreshed successfully using refresh_token grant" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next scheduled refresh:** 30 minutes from now" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ inputs.force_rebuild }}" == "true" ] || [ -n "${{ inputs.hytale_access_token }}" ]; then
            echo "🔨 Container rebuild triggered" >> $GITHUB_STEP_SUMMARY
          fi
